<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.farm.dao.UserMapper">
<select id="findByIdAndPsw" resultType="com.farm.pojo.User">
select * from farm where userId=#{userId} and userPassword=#{userPassword}
</select>
</mapper>